{
  "name": "hoozmo",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "prebuild": "mkdir -p public/lib && cp -r lib/* public/lib/",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@ruby/wasm-wasi": "^2.7.2"
  },
  "devDependencies": {
    "vite": "^7.3.0"
  }
}
